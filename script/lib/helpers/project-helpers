#!/usr/bin/env bash

run-preflight-os() {
    eval "$(script-root)/preflight-os"

}
script-root() {
    echo "$(project-root)/script"
}

project-root() {
    local _project_root=$PROJECT_ROOT
    echo $_project_root
}
