---
classes:
  - 'puppet::masterless'

st2::version: 0.13.0
st2::revision: 1
st2::ng_init: true

st2::packs:
  hubot:
    ensure: present
    config:
      endpoint: "http://localhost:8081"
  st2:
    ensure: present
